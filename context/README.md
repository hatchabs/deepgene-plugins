# context/

Reusable context documents (brand voices, sender-specific style overlays, project briefs, domain-specific phrasing) that skills compose in at runtime. The root-level `style-guide.md` is the default anti-AI-ese style context; project overlays live here and extend it without modifying it.
